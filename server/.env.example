# Server: ingestion API demo config

SDP_ENV=dev

# Postgres connection string (matches docker-compose)
DB_DSN=postgresql://sdp:sdp_password@postgres:5432/sdp

# Crypto key for encrypting PII in Postgres token_vault
# Should match the client's key only if you want symmetric decrypt behavior.
SDP_CRYPTO_KEY=Miql-SH11OTpm4rFOh5QF7iNG2fPolSwwvvb1YceREw=

# API key that clients must send in X-API-Key
SDP_API_KEY=dev-secret-api-key
